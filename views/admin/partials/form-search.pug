form(form-search )
            .input-group
              input(
                type="text"
                placeholder="Nhập từ khóa"
                name="inputValue"
                class="form-control "
              )
              .input-group-append
                button(
                  class="btn btn-success"
                  type="submit"
                ) Tìm